services:
  - type: web
    name: kiss-tracker-frontend
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://kiss-tracker-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html